import { Suspense } from "react";

export default function LocationPage() {
    return (
        <Suspense>
            <div>
                Location page
            </div>
        </Suspense>
    );
}
